@if (User.Identity.IsAuthenticated && User.IsInRole("TdAdministrators"))
{
    <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown">
            <i class="fa fa-gears"></i>
            <span class="caret"></span>
        </a>
        <ul class="dropdown-menu" role="menu">
            <li>
                @Html.ActionLink("Application Settings", "Index", "ApplicationSettings")
            </li>
            <li>
                @Html.ActionLink("Demo Data Management", "Demo", "DataManagement")
            </li>
            <li>
                @Html.ActionLink("Search Management", "Index", "SearchManager")
            </li>
        </ul>
    </li>
}
